# BB-INS3 — accumulated evidence log

Durable append-only record for roadmap item **BB-INS3** (Instrument render/depth/shader/pipeline identity and timing).

This file exists because the roadmap item body must stay reviewable as the item
accumulates slices. It carries the **verbatim** roadmap prose that was moved out
of [`ROADMAP.md`](../../ROADMAP.md) on 2026-08-25, and it is the place where
future slices append their evidence. The roadmap item keeps the current status,
the current unresolved question, the remaining slice budget, and a link here.

Nothing in this file is a new claim. Every statement below was already established
(or already marked unestablished) by the PRs listed in the roadmap item. Evidence
classes are unchanged: `static` + `synthetic` only, with no Bloodborne runtime
execution and no shadPS4 runtime producer.

## Verbatim roadmap prose as of commit `abc9b9b` (moved 2026-08-25)

### BB-INS3 — Instrument render/depth/shader/pipeline identity and timing
- **Status / priority / execution:** Partially implemented / High / CLOUD RESEARCH
- **Depends on:** BB-INS1
- **Question:** Как correlate render/depth resources with safe shader/pipeline IDs, creation/cache events and coarse CPU/GPU timings?
- **Result / evidence:** Static inspection at exact BB-BL1 baseline identified `PipelineCache::RefreshGraphicsKey`/`GetGraphicsPipeline` and `Scheduler::BeginRendering`/`SubmitExecution` as candidate graphics/pipeline/render-lifecycle seams. The earlier synthetic `bb-trace-events/v1` reconstruction correlates capture-local graphics events with pipeline IDs and separate CPU/GPU timing spans while preserving resource/queue/pipeline/span correlation. Deterministic source-bound shader identities, a bounded `pipeline_family_identity`, and render identities with explicit color/depth/stencil attachment roles are established synthetically from pinned source semantics. The complete pinned 21-field top-level/flattened `GraphicsPipelineKey` equality surface is inventoried in `docs/instrumentation/graphics-pipeline-key-surface.json`; exact canonicalization covers all 21/21 fields and the checker reports `pipeline_identity_ready=true`. `bb-graphics-identity/v2` defines an exact `pipeline_identity` from the complete canonical key, validates every canonical value against the committed surface rules, binds identity provenance to the pinned source baseline and SHA-256 of the validated surface document, and retains `pipeline_family_identity` as a separate coarser descriptor. `bb-graphics-pipeline-producer/v1` now defines a versioned static/synthetic admission contract for the future bounded `GetGraphicsPipeline` producer: it pins the exact BB-BL1 source seam plus identity/key-surface compatibility, requires exact `pipeline:sha256:...` identities, distinguishes `created` from `cache_hit`, enforces a strictly increasing capture-local sequence with bounded record count, and fails closed on stale or incompatible provenance. The source-integration preparation slice pins `src/video_core/renderer_vulkan/vk_pipeline_cache.cpp` at BB-BL1 commit `28c84fb5a7b19c7fb86156a1d6bb3e7e5a6cef64` and Git blob `b39f1c30bfb00d1f21a082da48369ba95ce31368`, then prepares an off-by-default `SHADPS4_BB_GRAPHICS_PIPELINE_OBSERVE(graphics_key, is_new)` hook immediately after the unique `graphics_pipelines.try_emplace(graphics_key)` lookup result without changing normal builds. `bb-graphics-pipeline-cpp-identity-vector/v1` now freezes the exact canonical UTF-8 JSON payload and expected `pipeline:sha256:c46cf5568ebdb232b52bc092f2fc445bf1fa9aee1b7663717b087fae5cce1c38` digest that an independent C++ implementation of `bb-graphics-identity/v2` must reproduce; the vector is bound to the pinned source baseline, the committed `bb-graphics-pipeline-key-surface/v12` version/digest, and the existing graphics-identity synthetic fixture. The independent C++20 conformance implementation now represents all 21 canonical key fields as typed C++ scalars/arrays/records, serializes the canonical payload without consuming the frozen payload as input, computes SHA-256 independently in C++, and reproduces both the frozen byte-exact payload and expected `pipeline:sha256:c46cf5568ebdb232b52bc092f2fc445bf1fa9aee1b7663717b087fae5cce1c38` identity. `bb-graphics-pipeline-cpp-source-mapping/v1` now binds every one of the 21 canonical fields exactly once to the pinned real BB-BL1 `Vulkan::GraphicsPipelineKey` source surface, including exact field-specific mapping modes/expressions and memberwise `Shader::PsColorBuffer` / `AmdGpu::BlendControl` mappings; the validator rejects declaration drift, wrong source expressions/modes, hidden fallback keys, record-member drift, and key-surface provenance drift using the exact committed `bb-graphics-pipeline-key-surface/v12` identity with platform-stable UTF-8/LF normalization. `bb-graphics-pipeline-production-emitter-admission/v1` now defines a fail-closed static/synthetic compatibility boundary for the production adapter/emitter patch: it pins the exact BB-BL1 `GetGraphicsPipeline` seam, requires the existing producer contract, complete 21-field source mapping, independent C++ exact-identity implementation and canonical key surface, keeps the integration compile-time-off-by-default, requires the real `graphics_key` as adapter input, derives `created` / `cache_hit` solely from the post-lookup `is_new`, and explicitly forbids `std::hash<GraphicsPipelineKey>`, raw object bytes or `memcmp` bytes as exact-identity substitutes. This remains static/synthetic admission evidence only: it does not modify shadPS4 or establish production runtime emission. Evidence is `static` + `synthetic`; no Bloodborne runtime execution, emitted exact cross-run pipeline identity from shadPS4, enabled runtime producer, independently observed creation/cache semantics, GPU timestamp semantics, target coverage, or measured instrumentation overhead is established.
- **Next experiment / information gain:** Implement the actual production C++ adapter/emitter patch admitted by `bb-graphics-pipeline-production-emitter-admission/v1` at the prepared `GetGraphicsPipeline` seam, compile it against the exact BB-BL1 baseline, and emit `bb-graphics-pipeline-producer/v1` records using the validated real-key mapping plus independently conformant typed serializer; then independently exercise `created`/`cache_hit` classification before promoting runtime evidence. Separately establish a bounded runtime timing producer with independently justified CPU/GPU timing semantics; do not treat optional Tracy GPU scopes as the BB timing source without independent evidence.
- **Acceptance / artifacts:** `tools/graphics_timing_trace.py`, `tests/test_graphics_timing_trace.py`, `docs/instrumentation/examples/graphics-timing.synthetic.json`, `.github/workflows/graphics-timing-trace.yml`, and `docs/instrumentation/graphics-timing.md` provide the capture-local reconstruction slice. `tools/graphics_identity_model.py`, `tests/test_graphics_identity_model.py`, `docs/instrumentation/examples/graphics-identity.synthetic.json`, and `.github/workflows/graphics-identity-model.yml` provide safe shader/render/pipeline-family identities plus the static/synthetic exact canonical pipeline-identity contract. `docs/instrumentation/graphics-pipeline-key-surface.json`, `tools/graphics_pipeline_key_surface.py`, `tests/test_graphics_pipeline_key_surface.py`, and `.github/workflows/graphics-pipeline-key-surface.yml` provide the complete exact pinned equality-surface canonicalization contract. `schemas/graphics-pipeline-producer.schema.json`, `tools/graphics_pipeline_producer_contract.py`, `tests/test_graphics_pipeline_producer_contract.py`, `docs/instrumentation/graphics-pipeline-producer.md`, `docs/instrumentation/examples/graphics-pipeline-producer.synthetic.json`, and `.github/workflows/graphics-pipeline-producer.yml` provide the versioned producer-admission compatibility slice. `tools/prepare_graphics_pipeline_producer_patch.py`, `tests/test_prepare_graphics_pipeline_producer_patch.py`, `docs/instrumentation/graphics-pipeline-producer-source-patch.md`, and `.github/workflows/graphics-pipeline-producer-patch.yml` provide the exact provenance-bound source-seam preparation slice. `tools/graphics_pipeline_cpp_identity_vector.py`, `tests/test_graphics_pipeline_cpp_identity_vector.py`, `docs/instrumentation/graphics-pipeline-cpp-identity-vector.md`, `docs/instrumentation/examples/graphics-pipeline-cpp-identity-vector.synthetic.json`, and `.github/workflows/graphics-pipeline-cpp-identity-vector.yml` provide the byte-exact C++ conformance-vector slice. `tools/graphics_pipeline_cpp_identity_conformance.cpp`, `tests/test_graphics_pipeline_cpp_identity_conformance.py`, `docs/instrumentation/graphics-pipeline-cpp-identity-conformance.md`, and `.github/workflows/graphics-pipeline-cpp-identity-conformance.yml` provide the independent typed C++20 conformance implementation and regression. `docs/instrumentation/graphics-pipeline-cpp-source-mapping.json`, `tools/graphics_pipeline_cpp_source_mapping.py`, `tests/test_graphics_pipeline_cpp_source_mapping.py`, `docs/instrumentation/graphics-pipeline-cpp-source-mapping.md`, and `.github/workflows/graphics-pipeline-cpp-source-mapping.yml` provide the exact pinned real-source→canonical mapping contract. `docs/instrumentation/graphics-pipeline-production-emitter-admission.json`, `tools/graphics_pipeline_production_emitter_admission.py`, `tests/test_graphics_pipeline_production_emitter_admission.py`, `docs/instrumentation/graphics-pipeline-production-emitter-admission.md`, and `.github/workflows/graphics-pipeline-production-emitter-admission.yml` provide the production-emitter patch-admission compatibility slice. Completion still requires an evidence-backed runtime producer emitting exact safe cross-run pipeline identity, independently supported creation/cache observations, a runtime timing producer with established CPU/GPU semantics, target coverage and measured tracing overhead; no proprietary shader payload may be committed.
- **Validation:** Exact-head CI for #120 validates the production-emitter admission contract against the pinned BB-BL1 `GetGraphicsPipeline` seam plus the current producer schema, 21-field real-key source mapping, independent C++ identity implementation and canonical key surface; it fail-closes on runtime-key/classification drift, forbidden identity-source weakening, hidden patch-bundle paths and unsupported runtime-claim promotion. This establishes static/synthetic admission compatibility only; it does not establish a production patch or runtime emission. Exact-head CI for #118 validates the pinned real-source mapping against exact BB-BL1 `vk_graphics_pipeline.h`, `runtime_info.h`, and `regs_color.h`, verifies all 21 canonical fields exactly once, fail-closes on the exact expected mapping mode/expression and record-member expressions, rejects wrong-source substitutions/hidden fallback keys/declaration drift, and verifies the platform-stable UTF-8/LF-normalized identity of the committed `bb-graphics-pipeline-key-surface/v12` document. This establishes static/synthetic source-mapping compatibility only; it does not establish production integration or runtime emission. Exact-head CI for #116 compiles the independent C++20 implementation with strict warnings, exercises typed representation/serialization of all 21 canonical key fields including 64-bit `stage_hashes` and typed color/blend records, and verifies byte-for-byte canonical payload plus expected SHA-256 identity against the committed vector. This establishes static/synthetic cross-language conformance only; it does not establish production shadPS4 mapping or runtime emission. Exact-head CI for #114 exercises regeneration of the frozen canonical payload from the committed Python model/fixture, byte-for-byte canonical UTF-8 JSON matching, expected SHA-256 identity matching, stale source/model/key-surface provenance rejection, unknown/non-canonical vector rejection, and dedicated workflow triggering across all material identity/surface inputs. This establishes internal static/synthetic cross-language conformance evidence only; independent evidence begins when a separate C++ implementation reproduces the committed bytes and digest. Exact-head CI for #98 exercises fail-closed BB-BL1 commit/Git-blob binding, unique exact source-seam matching, repeated-application rejection, single-hunk patch shape and exact upstream-source preparation for the off-by-default pipeline observer hook; exact-head CI for #96 exercises the producer-admission JSON Schema/semantic validator, strict increasing-sequence and bounded-volume rules, exact identity/source/seam compatibility, stale/incompatible provenance rejection, capture-local-ID rejection and synthetic fixture CLI validation. Together with the existing graphics-identity/key-surface checks, this establishes static/synthetic producer compatibility, source integration, conformance target, an independent C++ conformance implementation, exact pinned real-source mapping, and the production-emitter admission boundary only; it does not establish runtime emission, creation/cache runtime semantics, target coverage, GPU timestamp semantics, or target overhead.
- **Scope:** Medium
